{% extends "base.html.twig" %}

{% block title %}Editer l'emprunt
{% endblock %}

{% block body %}
	<h1>Editer l'emprunt</h1>
	{{form_start(form)}}
	{{form_widget(form)}}
	<button type="submit">
		Enregister</button>
	{{form_end(form)}}
	<a href="{{path('borrowing_index')}}">Retour à la liste</a>
{% endblock %}
